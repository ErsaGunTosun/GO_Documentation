package main

import "fmt"

func main() {
	var name string = "ErsaGun"
	// var(variable) variable_name variable_type = value

	var surname string
	surname = "Tosun"

	age := 20
	// var(variable) variable_name := value

	fmt.Println("Run", name, surname, age)
}
